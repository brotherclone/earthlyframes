= form_for @albumtype do |f|
  - if @albumtype.errors.any?
    #error_explanation
      %h2= "#{pluralize(@albumtype.errors.count, "error")} prohibited this albumtype from being saved:"
      %ul
        - @albumtype.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
