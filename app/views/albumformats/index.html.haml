%h1 Listing albumformats

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - @albumformats.each do |albumformat|
      %tr
        %td= link_to 'Show', albumformat
        %td= link_to 'Edit', edit_albumformat_path(albumformat)
        %td= link_to 'Destroy', albumformat, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Albumformat', new_albumformat_path
