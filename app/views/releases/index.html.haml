%h1 Listing releases

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - @releases.each do |release|
      %tr
        %td= link_to 'Show', release
        %td= link_to 'Edit', edit_release_path(release)
        %td= link_to 'Destroy', release, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Release', new_release_path
