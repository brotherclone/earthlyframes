require 'rails_helper'

RSpec.feature 'Albums', :type => :feature, :js=>true do
  describe 'It checks that albums have required content' do
    it 'checks the home page for cover image' do
      assert_raise('No test written')
    end
    it 'checks the home page for songs' do
      assert_raise('No test written')
    end
    it 'checks the home page for basic info' do
      assert_raise('No test written')
    end
    it 'checks the home page for links to buy' do
      assert_raise('No test written')
    end
    it 'checks the home page for sound cloud links' do
      assert_raise('No test written')
    end
  end
end