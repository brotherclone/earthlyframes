require 'rails_helper'

RSpec.feature 'Splash', :type => :feature, :js=>true do
  describe 'It checks that the home splash component is working' do
    it 'checks the splash video' do
      assert_raise('No test written')
    end
    it 'checks the splash back up image' do
      assert_raise('No test written')
    end
    it 'checks for the splash timing' do
      assert_raise('No test written')
    end
  end
end