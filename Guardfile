guard 'livereload', apply_css_live: false do
  watch(%r{app/views/.+\.(erb|haml)$})
  watch(%r{app/helpers/.+\.rb$})
  watch(%r{app/assets/builds/.+\.(css|js)$})
  watch(%r{public/.+\.(css|js|html)$})
end
